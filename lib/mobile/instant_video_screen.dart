import 'package:flutter/material.dart';

class InstantVideoScreen extends StatelessWidget {
  const InstantVideoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}